<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 희수 2020-01-23 -->
<mapper namespace="sensor">
	
	<update id="insert" parameterType="sen">
		update adboardtb 
		set temp=#{temp},humid=#{humid},dust=#{dust},rough=#{rough},info=#{info}
		where idx=1
	</update>

	<select id="selectall" resultType="monthtb">
		select * from monthtb
	</select>
	
	<select id="selectscore" parameterType="int" resultType="int">
		select weight from nationtb
		where idx=#{idx}
	</select>
	
	<select id="selectdust" parameterType="int" resultType="int">
		select dust from nationtb
		where idx=#{idx}
	</select>
	
	<update id="updateScore" parameterType="score">
		update nationtb 
		set weight=#{score} 
		where idx=#{idx}
	</update>
</mapper>